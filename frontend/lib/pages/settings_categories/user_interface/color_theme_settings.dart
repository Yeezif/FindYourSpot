import 'package:flutter/material.dart';

class ColorThemeSettings extends StatelessWidget {
  const ColorThemeSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}